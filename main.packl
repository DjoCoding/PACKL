func fibo(n: int): int {    
    if (!n) { fibo = 1; }
    else if (n == 1) { fibo = 1; }
    else {
        fibo = fibo(n - 1) + fibo(n - 2);
    }
}

proc main() {
    var n: int = 30;
    var result: int = fibo(n);
    write(0, "fibo(", n, ") = ", result, "\n");
    exit(0);
}