class Point {
    x: int;
    y: int;
};

func Point.setx(x: int): int {
    this.x = x;
}

proc Point.toString() {
    write(0, "(", this.x, ", ", this.y, ")\n");
}

proc main() {
    var p: Point = new Point;
    p.setx(10);
    p.toString();
}
