class Point {
    x: int;
    y: int;
};

proc Point.init(x: int, y: int) {
    this.x = x;
    this.y = y;
}

proc main() {
    var p: Point = new Point;
}

# parsing methods is done
# need to parse expression of object attributes and method calls
# need to generate the code for the object initialzation, object method call ...
# need somehow a way to print an object same as javascript