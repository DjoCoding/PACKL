func strlen(s: str): int {
    var i: int = 0;

    while(s[i++]) {
    }

    strlen = i - 1;
}

proc main() {
    var s: str = "PACKL";
    write(0, "the length of `", s, "` is: ", strlen(s), "\n");
}